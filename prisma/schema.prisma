// Point d’entrée principal : `generator` et `datasource`.
// Modèles et enums : voir `prisma/models/*.prisma`.

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}
